# Fithub Agent Environment Variables

# --- Mandatory Keys ---
# 1. Choose your LLM Provider: "huggingface" or "openai"
LLM_PROVIDER=huggingface

# 2. Hugging Face API Key (Required if provider is huggingface, also used for Embedding/Summarization)
HF_API_KEY=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 3. OpenAI API Key (Required if provider is openai)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Local Model Paths ---
# Path to the RepoGraph model directory
# Ensure this points to the correct location on your machine
LOCAL_MODEL_DIR=/Users/iyeonglag/PycharmProjects/2025-2-CSC4004-1-3-Fithub/models/RepoGraph

# --- Optional Services (Defaults provided) ---
# URL of the backend API (for fetching repo data)
BACKEND_API_URL=http://localhost:4000/api

# URL of the graph model server (if running separately)
GRAPH_MODEL_SERVER_URL=http://localhost:9000

# --- System Settings ---
# Directory for temporary repository storage
TEMP_DIR=./temp_repos

# --- 🛠️ LangSmith Tracing (Optional but Recommended) ---
# Enable tracing for debugging and monitoring
LANGCHAIN_TRACING_V2=true
LANGCHAIN_ENDPOINT="https://api.smith.langchain.com"
LANGCHAIN_API_KEY=lsv2_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LANGCHAIN_PROJECT="fithub-agent-v1"
